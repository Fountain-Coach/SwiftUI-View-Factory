name: Test Hetzner Runner

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: self-hosted  # 👈 only the built-in label
    steps:
      - name: Check environment
        run: |
          echo "Runner hostname: $(hostname)"
          uname -a
          docker version
          docker compose version
